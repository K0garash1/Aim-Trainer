import React from 'react'
import ReactDOM from 'react-dom/client'
import AimTrainerApp from './AimTrainerApp.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <AimTrainerApp />
  </React.StrictMode>,
)
